# Copy this file to `.env` and fill in your key.
# This key is for the DEMO CLIENT (harness/demo.py) that talks through the proxy to
# the real Anthropic API. The Gatekeep proxy itself holds no key — it forwards
# whatever `x-api-key` each client request already carries.
ANTHROPIC_API_KEY=sk-ant-your-key-here
