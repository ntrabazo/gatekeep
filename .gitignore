# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# Runtime data — never commit
audit.db
audit.db-wal
audit.db-shm
.env

# Cached eval datasets — fetched by harness/fetch_datasets.py, not redistributed
harness/data/

# Local tooling / editor state
.claude/
.wrangler/

# Stray local files
download.txt

# Internal build-process docs — kept locally, not part of the published product
CLAUDE.md
PLAN.md
ORCHESTRATION.md
memory.md
